name: Sovereign Media Player — Multi-Platform Build & Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-macos:
    name: Build macOS Universal App
    runs-on: macos-14

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: Lint Python Code
        run: |
          pip3 install flake8 --break-system-packages
          flake8 build_open_core.py

      - name: Compile ARM64 Binary
        run: |
          mkdir -p build
          swiftc \
            frontend_ui/OpenSovereignPlayerUI.swift \
            -framework Cocoa -framework AVKit -framework AVFoundation \
            -framework Metal -framework MetalKit \
            -O -whole-module-optimization \
            -target arm64-apple-macosx11.0 \
            -Xlinker -dead_strip \
            -o build/player_arm64

      - name: Compile x86_64 Binary
        run: |
          mkdir -p build
          swiftc \
            frontend_ui/OpenSovereignPlayerUI.swift \
            -framework Cocoa -framework AVKit -framework AVFoundation \
            -framework Metal -framework MetalKit \
            -O -whole-module-optimization \
            -target x86_64-apple-macosx10.15 \
            -Xlinker -dead_strip \
            -o build/player_intel

      - name: Create Universal Mach-O Binary
        run: |
          mkdir -p dist
          lipo -create -output build/SovereignPlayer_universal build/player_arm64 build/player_intel
          lipo -info build/SovereignPlayer_universal

      - name: Strip Debug Symbols
        run: strip -x -ur build/SovereignPlayer_universal

      - name: Bundle into .app
        run: python3 build_open_core.py --bundle-only

      - name: Package macOS Universal Zip
        run: |
          cd dist
          zip -r -9 SovereignPlayer_macOS_Universal.zip SovereignPlayer.app

      - name: Upload macOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SovereignPlayer-macOS
          path: dist/SovereignPlayer_macOS_Universal.zip
          retention-days: 30

  build-windows:
    name: Build Windows x64 Executable
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build_win -S . -DCMAKE_BUILD_TYPE=Release

      - name: Build with MSVC
        run: cmake --build build_win --config Release

      - name: Package Windows ZIP
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist_win
          Copy-Item build_win\Release\SovereignPlayer.exe dist_win\
          if (Test-Path build_win\Release\sovereign_core.dll) {
              Copy-Item build_win\Release\sovereign_core.dll dist_win\
          }
          Copy-Item core_engine\include\sovereign_engine.h dist_win\
          $readme = @(
              "================================================================================",
              "  SOVEREIGN MEDIA PLAYER - WINDOWS x64 EDITION",
              "  (C) 2026 Sovereign Byte Technology. All Rights Reserved.",
              "================================================================================",
              "QUICK START:",
              "  1. Extract all files to a folder on your computer (Right-click -> Extract All).",
              "  2. Double-click SovereignPlayer.exe to launch.",
              "",
              "STANDALONE ENGINE:",
              "  - SovereignPlayer.exe is a 100% self-contained, standalone binary with",
              "    the core engine statically linked (no external DLL dependencies needed).",
              "  - For software developers: sovereign_core.dll & sovereign_engine.h are",
              "    provided for embedding into third-party C# / C++ applications.",
              "================================================================================"
          )
          $readme | Set-Content -Path dist_win\README.txt -Encoding utf8
          Compress-Archive -Path dist_win\* -DestinationPath SovereignPlayer_Windows_x64.zip

      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SovereignPlayer-Windows
          path: SovereignPlayer_Windows_x64.zip
          retention-days: 30

  build-linux:
    name: Build Linux x86_64 Package
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: sudo apt-get update && sudo apt-get install -y build-essential cmake

      - name: Configure and Compile
        run: |
          cmake -B build_linux -S . -DCMAKE_BUILD_TYPE=Release
          cmake --build build_linux -j$(nproc)
          strip -s build_linux/SovereignPlayer build_linux/libsovereign_core.so

      - name: Package Linux Tarball
        run: |
          mkdir -p dist_linux
          cp build_linux/SovereignPlayer dist_linux/
          cp build_linux/libsovereign_core.so dist_linux/
          cp core_engine/include/sovereign_engine.h dist_linux/
          tar -czvf SovereignPlayer_Linux_x86_64.tar.gz -C dist_linux .

      - name: Upload Linux Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SovereignPlayer-Linux
          path: SovereignPlayer_Linux_x86_64.tar.gz
          retention-days: 30

  build-android:
    name: Build Android NDK Native Library
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        with:
          ndk-version: r25c

      - name: Build Android ARM64
        run: |
          cmake -B build_android_arm64 \
            -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_ROOT/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-21 \
            -S .
          cmake --build build_android_arm64 -j$(nproc)

      - name: Build Android APK and NDK Distribution
        run: |
          mkdir -p dist_android/arm64-v8a
          cp build_android_arm64/libsovereign_android.so dist_android/arm64-v8a/
          cp android/SovereignPlayer.kt dist_android/
          cp android/src/tech/sovereign/player/SovereignPlayer.java dist_android/
          cp core_engine/include/sovereign_engine.h dist_android/
          zip -r SovereignPlayer_Android_NDK.zip dist_android

          BUILD_TOOLS=$(ls -d $ANDROID_HOME/build-tools/* | sort -V | tail -n 1)
          PLATFORM_JAR=$(ls -d $ANDROID_HOME/platforms/android-*/android.jar | sort -V | tail -n 1)
          echo "Using Build Tools: $BUILD_TOOLS"
          echo "Using Platform JAR: $PLATFORM_JAR"

          mkdir -p android_build/classes
          javac -cp "$PLATFORM_JAR" -d android_build/classes android/src/tech/sovereign/player/*.java
          $BUILD_TOOLS/d8 --lib "$PLATFORM_JAR" --output android_build/ android_build/classes/tech/sovereign/player/*.class
          $BUILD_TOOLS/aapt package -F android_build/unaligned.apk -M android/AndroidManifest.xml -I "$PLATFORM_JAR"
          (cd android_build && zip -u unaligned.apk classes.dex)
          mkdir -p android_build/lib/arm64-v8a
          cp build_android_arm64/libsovereign_android.so android_build/lib/arm64-v8a/
          (cd android_build && zip -u -r unaligned.apk lib/)
          $BUILD_TOOLS/zipalign -v -p 4 android_build/unaligned.apk SovereignPlayer_Android.apk
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=AndroidDebug,O=Android,C=US"
          $BUILD_TOOLS/apksigner sign --ks debug.keystore --ks-pass pass:android --ks-key-alias androiddebugkey --key-pass pass:android SovereignPlayer_Android.apk

      - name: Upload Android Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SovereignPlayer-Android
          path: |
            SovereignPlayer_Android_NDK.zip
            SovereignPlayer_Android.apk
          retention-days: 30

  release:
    name: Create GitHub Multi-Platform Release
    runs-on: ubuntu-latest
    needs: [build-macos, build-windows, build-linux, build-android]
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download macOS Artifact
        uses: actions/download-artifact@v4
        with:
          name: SovereignPlayer-macOS

      - name: Download Windows Artifact
        uses: actions/download-artifact@v4
        with:
          name: SovereignPlayer-Windows

      - name: Download Linux Artifact
        uses: actions/download-artifact@v4
        with:
          name: SovereignPlayer-Linux

      - name: Download Android Artifact
        uses: actions/download-artifact@v4
        with:
          name: SovereignPlayer-Android

      - name: Publish Multi-Platform Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            SovereignPlayer_macOS_Universal.zip
            SovereignPlayer_Windows_x64.zip
            SovereignPlayer_Linux_x86_64.tar.gz
            SovereignPlayer_Android_NDK.zip
            SovereignPlayer_Android.apk
          name: "Sovereign Baseline Media Player ${{ github.ref_name }} (Open-Core Multi-Platform)"
          body: |
            ## Sovereign Media Player ${{ github.ref_name }} — Baseline Open-Core Release

            > **⚠️ Note:** This release contains the **Baseline Video Player only (main branch)**.
            > All V3 Advanced Multitrack Studio timeline and editor code is strictly housed in a separate private repository.

            **Built by Sovereign Byte Technology · Open-Core Edition**

            ### 🌟 What's New in ${{ github.ref_name }}:
            - **Windows Standalone Portability (Fixes #3)**: Statically linked core engine and MSVC runtime (`/MT`) for zero-dependency execution out of the box (resolves `sovereign_core.dll was not found` error).
            - **Docked UI & Custom Control Icons**: Seamless docked playback toolbar with high-contrast custom player icons and on-screen click play/pause.
            - **Telemetry HUD**: Live hardware decoding stats, FPS, CPU/GPU usage, and rendering pipeline indicators.
            - **Subtitles & Updater**: Restored Closed-Captions (`CC`) toggle and in-app update checker.

            ### 📦 Multi-Platform Downloads:
            | Platform | File | Description / Technology |
            | :--- | :--- | :--- |
            | **macOS** | `SovereignPlayer_macOS_Universal.zip` | Universal App (Apple Silicon M1–M4 + Intel Core i5/i7) · Metal Direct |
            | **Windows** | `SovereignPlayer_Windows_x64.zip` | Windows 10/11 (`x86_64`) · Standalone Zero-Dependency EXE + C/C++ SDK |
            | **Linux** | `SovereignPlayer_Linux_x86_64.tar.gz` | Ubuntu / Debian / Fedora (`x86_64`) · Vulkan / OpenGL / X11 |
            | **Android (APK)** | `SovereignPlayer_Android.apk` | Standalone Installable APK (Android 5.0+) |
            | **Android (NDK)** | `SovereignPlayer_Android_NDK.zip` | NDK Native Library (`arm64-v8a`) + Kotlin/Java SDK |

            ### 🚀 Quick Start
            - **macOS**: Unzip and drag `SovereignPlayer.app` to `/Applications`.
            - **Windows**: Unzip and run `SovereignPlayer.exe` (100% standalone, no external DLLs or runtimes required).
            - **Linux**: Extract with `tar -xvf SovereignPlayer_Linux_x86_64.tar.gz` and run `./SovereignPlayer`.
            - **Android (Direct)**: Download and install `SovereignPlayer_Android.apk`.
            - **Android (NDK/SDK)**: Extract `SovereignPlayer_Android_NDK.zip` to embed `libsovereign_android.so` in your app.

        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
